--- /tmp/m2-v4-work/M2_FORMAL_DEPENDENCY_CLOSURE_V2/backend/validation/providers.py	2026-08-14 19:52:58.000000000 +0800
+++ backend/validation/providers.py	2026-08-14 20:20:44.705369947 +0800
@@ -47,15 +47,14 @@
         temperature: float = 0.0,
         max_tokens: int = 4_000,
     ) -> None:
-        # Keep the deterministic in-process provider usable without installing
-        # an external provider SDK.  The SDK is required only when this adapter
-        # is instantiated.
-        from openai import OpenAI
-
         _validate_base_url(base_url)
         api_key = os.environ.get(api_key_env)
         if not api_key:
             raise RuntimeError(f"required provider credential is missing: {api_key_env}")
+        # Import the external SDK only after configuration validation. Static
+        # runs and fail-closed configuration checks do not require the SDK.
+        from openai import OpenAI
+
         self.provider_id = provider_id
         self.model = model
         self._base_url = base_url.rstrip("/")
