这是零碳云的最小产品演示。
第一步，AI读取一份合成电费数据，提出用电量、报告期间和所属设施等字段候选。
AI不会直接把结果写进正式账本，使用者必须先核对原始文件和识别字段。
点击确认后，系统锁定本次人工确认的完整内容，并生成确认指纹，防止字段在确认后被替换。
确定性代码随后使用演示因子计算排放结果。
这条具体结果会被带入工厂碳数据护照，系统高亮刚刚确认的记录，但不会自动归集或发布。
最后由人点击归集，完成从源文件、人工确认、确定性计算到可追溯碳护照的最小闭环。
本演示全部使用合成数据，不构成监管申报或法定核查。
